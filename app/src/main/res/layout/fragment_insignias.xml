<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/background_color"
    tools:context=".Insignias">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Imagen de la insignia -->
        <ImageView
            android:id="@+id/imagen_insignia"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@mipmap/insignia"
            android:contentDescription="Insignia"
            android:layout_marginBottom="16dp" />

        <!-- Nombre de la persona -->
        <TextView
            android:id="@+id/nombre_persona"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nombre de la Persona"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <!-- Mensaje de felicitación -->
        <TextView
            android:id="@+id/mensaje_felicidades"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="¡Felicidades! Has recibido una insignia."
            android:textSize="18sp"
            android:textColor="#4CAF50" />

    </LinearLayout>

</FrameLayout>
